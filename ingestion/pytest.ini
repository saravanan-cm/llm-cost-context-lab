[pytest]
testpaths = tests
pythonpath = ..
markers =
    integration: requires network access, OPENAI_API_KEY and a running Qdrant (set RUN_INTEGRATION=1)
filterwarnings =
    ignore:Payload indexes have no effect in the local Qdrant:UserWarning
